node_modules/
dist/
.DS_Store
*.local
.claude
CLAUDE.md

# Env
.env
.env.local
.env.*.local

# TS incremental
*.tsbuildinfo

# Screenshot pipeline — raw captures + framed output. Final framed assets
# live in android/fastlane/metadata + ios/fastlane/screenshots and are
# committed; the artefacts below are regenerable from `pnpm screenshots`.
/screenshots/
/output/
